---
label: Footer
hide_body: true
fields:
- name: logo
  type: file
  config:
    maxSize: 64
  label: Logo
- name: kontakt
  type: field_group
  config: {}
  fields:
  - name: icon
    type: file
    config:
      maxSize: 64
    label: icon
  - name: kontakt
    type: textarea
    default: ''
    config:
      required: false
      wysiwyg: true
      schema:
        format: html-blocks
    label: kontakt
  label: kontakt
- name: footernav
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: true
    schema:
      format: html-blocks
  label: footernav
pages:
- content/global/footer.md
