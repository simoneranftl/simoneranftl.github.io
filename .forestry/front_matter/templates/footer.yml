---
label: Footer
hide_body: true
fields:
- name: logo
  type: file
  config:
    maxSize: 64
  label: Logo
- name: footernav
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: true
    schema:
      format: html-blocks
  label: footernav
- name: kontakt
  type: field_group_list
  fields:
  - name: icon
    type: file
    config:
      maxSize: 64
    label: icon
  - name: text
    type: textarea
    default: ''
    config:
      required: false
      wysiwyg: true
      schema:
        format: html-blocks
    label: text
  config:
    min: 
    max: 
    labelField: 
  label: Kontakt
pages:
- content/global/footer.md
